const router = require("express").Router();
// retrive all wanted controllers 

// //get the all dishes form a store
// router.get('/dish/all',);

// //get a sepcific dish from a store
// router.get('/dish/:id',);

// // Make a single dish sold out or in stock with id
// router.put("/dish/soldout/:id",);

// // Make a single dish valid or invalid with id
// router.put("/dish/valid/:id",);

// // Edit a dish's all information by its id
// router.put("/dish/edit/:id",);

// // Add a dish
// router.post("/dish/add",);

// // Delete a dish
// router.delete("/dish/delete/:id",);

// // Retrieve all Orders in a specific date with page and limit
// router.get("/order/bydate/:date",);

// // Retrieve numbers of prder in a specific date
// router.get("/order/bydate/total",);

// // Get a basic order summary for a store
// router.get("/order/summary",);

// // Retrieve a single Order with id
// router.get("/order/:id", );

// // Make a order finished
// router.put("/order/status/:id",);



module.exports = router;